<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Hashed WiFi SSID is used as the bandit's environmental context
      (PRD section 3.1). The raw SSID is never persisted or transmitted;
      ContextManager only ever sees a sha256 prefix. Location permission
      is required because, since Android 8 (API 26), the WiFi name is
      gated behind ACCESS_FINE_LOCATION. We request "when in use" only.
    -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
      INTERNET is required for the two parent-initiated network actions: the
      voluntary "Check for updates" OTA fetch (ADR 0017) and the "Send feedback"
      POST (ADR 0018). Both are disclosed on kinderhorizon.org/lighthouse/privacy
      and never run automatically. The Flutter template declares INTERNET only in
      the debug/profile manifests (so the dev tooling connects), which silently
      omits it from RELEASE builds; declaring it here is what makes OTA and
      feedback work in a shipped build. (Crash-log "send" uses a mail-composer
      intent and needs no socket, so it was unaffected.)
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Microphone: the parent (behind the math gate) may record a short custom
      voice clip for a tile (ADR 0019). The recording stays on the device and is
      never transmitted, never in a crash report or feedback payload (disclosed
      on kinderhorizon.org/lighthouse/privacy). A child cannot start a recording.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
      RECORD_AUDIO makes Google Play imply android.hardware.microphone as a
      REQUIRED feature, which would drop devices without a mic (some tablets).
      Custom-voice recording is optional and math-gated; the app is fully usable
      without it, so the microphone is explicitly NOT required. Without this the
      Play Console flags a loss of supported devices vs. the previous release.
    -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:label="Lighthouse AAC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets flutter_email_sender resolve a mail composer on Android 11+
             (package visibility). Used by the parent-initiated "Send crash
             logs" action; no automatic send (ADR 0002). -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
